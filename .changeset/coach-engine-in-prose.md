---
kind: changed
---
A read now says when the prose writes out a choice, an effect, or a condition the scene doesn't carry, and where it belongs. It proposes a variable only with the effect or condition the engine would run, so a mood is no longer offered as one.
