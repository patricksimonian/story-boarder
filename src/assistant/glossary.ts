/**
 * The story's own words, from CONTEXT.md, so every rubric speaks them.
 * Kept short: the model needs the nouns, not the design notes.
 */
export const GLOSSARY = `Vocabulary, which you use and do not translate: a story is the whole project. A scene is the atomic unit — one time and place, with a synopsis, beats, and prose. A beat is the smallest unit of story change, ordered inside a scene. A storyline is an ordered sequence of scenes tracing one thread; an act is a time-grouping shared across storylines. The idea pool holds scenes in no storyline. A note is a freeform page of the story's notebook. Characters, places, and lore are the library's pages. A variable is a typed value in the story's registry; a condition gates a scene or a choice on variables; an effect changes a variable when a scene fires or a choice is taken; a choice is a directed link from one scene to another.`
